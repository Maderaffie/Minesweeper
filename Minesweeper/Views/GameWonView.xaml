<Window x:Class="Minesweeper.Views.GameWonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper.Views"
        mc:Ignorable="d"
        Title="GameWonView" Height="450" Width="800" WindowStartupLocation="CenterOwner" 
        WindowStyle="None" AllowsTransparency="True" FontFamily="Quicksand">
    <Window.Background>
        <SolidColorBrush Opacity="0.9" Color="Black"/>
    </Window.Background>
    <StackPanel>
        <Viewbox>
            <Label Foreground="BlanchedAlmond">
                Congratulations!
            </Label>
        </Viewbox>
        <TextBlock Foreground="BlanchedAlmond" HorizontalAlignment="Center" FontSize="20" Text="{Binding Time}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 130 0 0">
            <Button x:Name="NewGameButton" Margin="10" IsDefault="True" Command="{Binding SetDialogResultTrueCommand}" CommandParameter="{x:Reference NewGameButton}">
                <Viewbox>
                    <Label>
                        New Game
                    </Label>
                </Viewbox>
            </Button>
            <Button Margin="10" IsCancel="True">
                <Viewbox>
                    <Label>
                        Quit
                    </Label>
                </Viewbox>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
